# System Design

High-level system design documents illustrating tier separation, component responsibilities, and external dependencies.

## Key Concepts
- Server Components vs. Client Components separation.
- Decoupling business logic (services) from presentation (UI) and transport (HTTP/actions).
- Data access patterns via Prisma and Supabase.
