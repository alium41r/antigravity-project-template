# Docker Configurations

Container configurations:
- Multi-stage Dockerfile for Next.js production builds.
- `docker-compose.yml` for running local auxiliary services (PostgreSQL, Redis, local Supabase stack).
