# Public Static Assets

Standard Next.js public directory. Files placed here are served directly from the root URL (e.g. `public/favicon.ico` is served at `/favicon.ico`).

## Typical Contents
- `favicon.ico`, `robots.txt`, `sitemap.xml`
- Brand logos, public illustrations, and social share cards (Open Graph images)
