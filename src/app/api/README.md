# API Route Handlers (`src/app/api/`)

HTTP route handlers (`route.ts`) for:
- External webhooks (Stripe webhook listeners, Supabase event webhooks).
- Public REST endpoints.
- Health checks (`/api/health`).
